# 1-Translator-Agent

A simple Python-based translation agent built with Chainlit that uses an LLM (e.g., OpenAI) to translate text interactively or via script.  
*(Add a short project overview here — what the agent does, why it exists, and the main use-cases.)*

## 🔍 Features

- 🧠 AI-powered text translation using large language models  
- 💬 Interactive UI via **Chainlit** for real-time translation conversations  
- ⚙️ Easily configurable with environment variables  
- 📁 Basic CLI usage for batch or single translations  
- 🧪 Setup for local development and testing

## 🚀 Getting Started

### 🧩 Prerequisites

Before you begin, ensure you have:

- Python 3.10 or newer
- A supported AI API key (e.g., OpenAI API key)
- Git (optional, for cloning)

### 📥 Install

1. Clone the repository:

   ```bash
   git clone https://github.com/Mudasic1/1-translator-agent.git
   cd 1-translator-agent
